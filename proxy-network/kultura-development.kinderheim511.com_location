# Serve images and videos as static files directly from Nginx
location ~* ^/(images|videos)/ {
    root /usr/share/nginx/static;
    add_header Cache-Control "public, max-age=2592000, immutable";
    add_header Accept-Ranges bytes;
    try_files $uri =404;
}
